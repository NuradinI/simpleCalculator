def addNum(x, y):
    return x + y
    #def defines a function, addNum is the name of the function, in() we take in two arguments, the colon is like the {} in javascirpt
#the arguments defined are variables that will be inputed by the user, and the return is same as js